version: '3.5'

services:
  websocket:
    image: "websocket-frontend-image"
    container_name: "websocket-frontend-container"
    ports:
      - "7070:80"
    networks:
      static-network:
        ipv4_address: 10.5.0.6

networks:
  static-network:
    driver: bridge
    ipam:
      config:
        - subnet: 10.5.0.0/16
          gateway: 10.5.0.1
