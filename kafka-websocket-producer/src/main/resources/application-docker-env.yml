server:
  port: 8080

topic:
  name: t.message.order

app:
  interval-time: ${INTERVAL_TIME:-60000}
  data-file: ${DATA_FILE:-/temp/}

spring:
  kafka:
    producer:
      bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAPSERVERS:-10.5.0.3:9092}
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      group-id: "group_id"
